<p id="notice"><%= notice %></p>




<h1>Tareas </h1> <br>
  <div class="row">
    
  <% @works.each do |work| %>
  <div class="col-md-4">
    <div class="card" style="width: 18rem;">
  <%= image_tag work.photo, class: "card-img-top" %>
    
      <div class="card-body">
    <h5 class="card-title"><%= work.name %></h5>

    
    <a href="<%= work_orders_path(work) %>" class="btn btn-danger " data-method="post">Incompleto</a>
    <%= link_to 'Mostrar', work, class: "btn btn-primary" %>
    
      </div>
  </div>

  </div>

  <% end %> 

  <div>
    

  

  </div>
